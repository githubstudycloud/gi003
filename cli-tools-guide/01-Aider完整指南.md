# Aider 完整使用指南

## 目录
- [概述](#概述)
- [安装与配置](#安装与配置)
- [基础用法](#基础用法)
- [聊天模式详解](#聊天模式详解)
- [In-Chat命令大全](#in-chat命令大全)
- [高级功能](#高级功能)
- [脚本化与自动化](#脚本化与自动化)
- [最佳实践](#最佳实践)
- [故障排查](#故障排查)

---

## 概述

### 什么是Aider?

Aider是一款运行在终端中的AI结对编程工具,被归类为**Level 4自主代理**:
- ✅ 执行多步骤计划,最少监督
- ✅ 在失败时迭代调整
- ✅ 完成整个功能的开发

### 核心特点

| 特性 | 说明 |
|------|------|
| **Git原生集成** | 自动提交更改,智能生成commit消息 |
| **代码库映射** | 自动生成整个代码库的结构图 |
| **多模型支持** | 支持Claude、GPT、DeepSeek、本地模型等 |
| **上下文管理** | 智能管理聊天上下文,避免token浪费 |
| **多模态输入** | 支持图片、网页、语音输入 |

### 推荐模型(2025)

```bash
# 最佳性能
aider --model sonnet  # Claude 3.7 Sonnet

# 高性价比
aider --model deepseek  # DeepSeek R1 & Chat V3

# OpenAI最新
aider --model o3-mini  # o3-mini

# 本地模型
aider --model ollama/codellama  # 通过Ollama
```

---

## 安装与配置

### 快速安装

```bash
# 方式1: 推荐安装器
python -m pip install aider-install
aider-install

# 方式2: 直接安装
pip install aider-chat

# 方式3: 使用pipx(隔离环境)
pipx install aider-chat
```

### API密钥配置

```bash
# 设置API密钥环境变量
export ANTHROPIC_API_KEY=sk-ant-xxxxx
export OPENAI_API_KEY=sk-xxxxx
export DEEPSEEK_API_KEY=sk-xxxxx

# 或在命令行直接指定
aider --api-key anthropic=sk-ant-xxxxx
```

### 配置文件

#### 方式1: YAML配置文件

创建 `~/.aider.conf.yml` (全局配置):
```yaml
# 基本设置
model: claude-3-5-sonnet-20250219
editor-model: gpt-4o

# Git设置
auto-commits: true
dirty-commits: false
attribute-commits: true

# 行为设置
yes-always: false  # 生产环境建议false
dark-mode: true
pretty: true
stream: true
show-diffs: true

# 文件监控
watch-files: true

# 上下文管理
map-tokens: 1024
```

或项目级配置: `.aider.conf.yml` (放在git仓库根目录)

#### 方式2: 环境变量

```bash
# 单个选项
export AIDER_MODEL=claude-3-5-sonnet-20250219
export AIDER_AUTO_COMMITS=true
export AIDER_DARK_MODE=true

# 批量选项
export AIDER_FLAGS="yes-always,dark-mode,no-auto-commits"
```

---

## 基础用法

### 启动Aider

```bash
# 1. 基本启动(在项目目录)
aider

# 2. 指定要编辑的文件
aider src/main.py tests/test_main.py

# 3. 指定模型
aider --model deepseek src/app.py

# 4. 只读模式(不会修改文件)
aider --read src/utils.py

# 5. 使用通配符
aider src/**/*.py

# 6. 自动确认模式
aider --yes-always src/auth.py
```

### 添加文件到对话

```bash
# 启动后添加文件
aider
> /add src/models.py
> /add src/views.py

# 使用通配符
> /add src/**/*.js

# 添加只读文件(作为参考)
> /read-only docs/api.md
```

### 基本对话流程

```bash
aider src/app.py

# AI会自动映射代码库结构
# 然后你可以提出需求:

> 添加用户认证功能,使用JWT令牌

# Aider会:
# 1. 分析当前代码结构
# 2. 提出实施方案
# 3. 编辑文件
# 4. 自动Git提交

# 查看更改
> /diff

# 撤销更改
> /undo
```

---

## 聊天模式详解

Aider支持多种聊天模式,适用于不同场景。

### Code模式(默认)

**用途**: 编辑代码

```bash
# 自动使用code模式
> 重构login函数,使用async/await

# 明确切换到code模式
> /code
```

**特点**:
- ✅ 直接编辑文件
- ✅ 自动提交到Git
- ✅ 适合实际开发任务

### Ask模式

**用途**: 询问问题,不编辑代码

```bash
# 进入ask模式
> /ask

# 或直接使用/ask前缀
> /ask 这个函数的时间复杂度是多少?
> /ask 如何优化这段代码?
> /ask 解释一下这个设计模式
```

**特点**:
- ✅ 仅获取建议,不修改文件
- ✅ 适合代码审查、学习、讨论
- ✅ 不会消耗Git提交

### Architect模式(双模型协作)

**用途**: 复杂任务的高级规划

```bash
# 启动architect模式
aider --architect

# 或在聊天中切换
> /architect

# 使用architect模式执行任务
> /architect 重构整个认证系统,支持OAuth2和多因素认证
```

**工作原理**:
1. **Architect模型**(如o1-preview): 制定高层方案
2. **Editor模型**(如DeepSeek或o1-mini): 将方案转为具体代码编辑

**性能**:
- 在代码编辑基准测试中达到**85% SOTA分数**
- 推荐配置: `o1-preview(Architect) + DeepSeek/o1-mini(Editor)`

**示例配置**:
```yaml
# .aider.conf.yml
model: o1-preview        # Architect
editor-model: deepseek   # Editor
```

### 模式切换最佳实践

**推荐工作流**:
```bash
# 1. 先用ask模式讨论方案
> /ask 添加缓存层的最佳方式是什么?

# 2. 根据AI建议选择方案
> /ask 比较Redis和Memcached,哪个更适合?

# 3. 确定方案后切换到code模式
> /code

# 4. 执行实施
> 好的,使用Redis实现缓存层,参考刚才的建议

# 5. 对于复杂重构,使用architect模式
> /architect 将单体应用拆分为微服务架构
```

---

## In-Chat命令大全

所有命令以 `/` 开头,可以在Aider运行时使用。

### 文件管理命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/add <文件>` | 添加文件到对话 | `/add src/auth.py` |
| `/drop <文件>` | 从对话中移除文件 | `/drop src/old_code.py` |
| `/read-only <文件>` | 添加只读文件(作为参考) | `/read-only docs/api.md` |
| `/ls` | 列出当前对话中的所有文件 | `/ls` |

**技巧**:
```bash
# 使用通配符
> /add src/**/*.py

# 在项目根目录外添加文件
> /add ../shared/utils.py

# 移除不需要的文件减少token消耗
> /drop tests/**/*.py
```

### Git命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/commit [消息]` | 手动提交更改 | `/commit 添加了用户认证` |
| `/undo` | 撤销最后一次更改 | `/undo` |
| `/diff` | 显示未提交的更改 | `/diff` |
| `/clear` | 清空对话历史(保持文件) | `/clear` |
| `/reset` | 重置对话,移除所有文件 | `/reset` |

**高级用法**:
```bash
# 查看最近的提交
$ git log --oneline -5

# 撤销多次更改
> /undo
> /undo
> /undo

# 查看详细diff
> /diff
```

### 上下文管理命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/tokens` | 显示当前token使用情况 | `/tokens` |
| `/clear` | 清空对话历史 | `/clear` |
| `/reset` | 完全重置,移除所有文件和历史 | `/reset` |

**最佳实践**:
```bash
# 定期检查token使用
> /tokens
# Output: 12,450 tokens used (62% of limit)

# 当token接近上限时清理
> /clear

# 或移除不必要的文件
> /drop src/unused.py
```

### 模式切换命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/code` | 切换到code模式 | `/code` |
| `/ask [问题]` | 切换到ask模式或直接提问 | `/ask 这段代码有什么问题?` |
| `/architect [任务]` | 切换到architect模式 | `/architect 重构数据库层` |

### 设置与配置命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/model <模型名>` | 切换AI模型 | `/model deepseek` |
| `/help` | 显示帮助信息 | `/help` |
| `/settings` | 显示当前设置 | `/settings` |
| `/exit` 或 `/quit` | 退出Aider | `/exit` |

**模型切换示例**:
```bash
# 切换到更便宜的模型做简单任务
> /model gpt-3.5-turbo

# 切换到强大模型做复杂任务
> /model o1-preview

# 切换到本地模型
> /model ollama/codellama
```

### 编辑器命令

| 命令 | 说明 | 快捷键 |
|------|------|--------|
| `/editor` | 打开文本编辑器编写多行消息 | `Ctrl-X Ctrl-E` |
| `/paste` | 粘贴剪贴板内容 | - |

**使用场景**:
```bash
# 编写复杂的多行需求
> /editor
# 会打开$EDITOR编辑器,编写完成后保存关闭

# 粘贴错误日志
> 这是错误:
> /paste
```

### 实用工具命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/web <URL>` | 添加网页内容到对话 | `/web https://docs.python.org/3/library/asyncio.html` |
| `/run <命令>` | 运行shell命令并查看输出 | `/run pytest tests/` |

---

## 高级功能

### 1. 代码库映射

Aider会自动为整个代码库生成"地图",帮助AI理解项目结构。

```bash
# 启动时自动生成映射
aider

# Aider会扫描:
# - 文件结构
# - 函数/类定义
# - 依赖关系
```

**控制映射大小**:
```yaml
# .aider.conf.yml
map-tokens: 2048  # 增加映射的token预算
```

**最佳实践**:
- 在大型项目中,映射帮助AI"定位"相关代码
- 即使没有明确添加文件,AI也能引用项目中的其他文件

### 2. 图片与多模态输入

```bash
# 方式1: 添加图片文件
aider
> /add screenshot.png
> 根据这个设计图实现UI

# 方式2: 粘贴剪贴板图片
> 将图片复制到剪贴板
> /paste
> 修复这个界面bug

# 方式3: 添加网页
> /web https://example.com/api-docs
> 根据这个API文档实现客户端
```

**应用场景**:
- 📸 UI设计图转代码
- 🐛 截图辅助bug修复
- 📖 参考文档实现功能

### 3. 语音输入

```bash
# 使用语音描述需求
aider --voice

# 或在配置中启用
# .aider.conf.yml
voice: true
```

说话描述任务,Aider会转录并执行。

### 4. IDE集成(文件监控)

在你喜欢的IDE中工作,Aider会自动检测文件变化。

**方式1: 使用#AI注释**

```python
# src/auth.py

# #AI 添加密码重置功能
def reset_password(email):
    pass
```

保存文件,Aider会自动检测到 `#AI` 注释并执行任务。

**方式2: Watch模式**

```yaml
# .aider.conf.yml
watch-files: true
```

Aider会监控对话中的文件,检测到 `#AI` 注释时自动执行。

**工作流**:
1. 在IDE中编写代码
2. 添加 `#AI <任务描述>` 注释
3. 保存文件
4. Aider自动完成任务
5. 在IDE中看到结果

### 5. Git高级集成

```bash
# 自定义commit消息格式
aider --commit-prompt "请生成符合Conventional Commits规范的消息"

# 禁用自动提交(手动控制)
aider --no-auto-commits

# 允许在脏工作区提交
aider --dirty-commits

# 在commit中署名AI
aider --attribute-commits
# Commit消息会包含: "Co-authored-by: aider <aider@aider.chat>"
```

**Git工作流示例**:
```bash
# 1. 创建feature分支
git checkout -b feature/user-auth

# 2. 启动aider
aider --auto-commits src/auth.py

# 3. 描述功能
> 实现JWT认证,包括登录、注册、令牌刷新

# 4. Aider自动提交每个步骤
# git log会显示:
# - feat: add JWT token generation
# - feat: implement login endpoint
# - feat: add token refresh logic

# 5. 审查后合并
git diff master
git push origin feature/user-auth
```

### 6. Prompt缓存(节省成本)

某些模型(如Claude)支持prompt缓存,可以大幅降低成本。

```yaml
# .aider.conf.yml
cache-prompts: true
```

**效果**:
- 重复的系统提示词不会重复计费
- 代码库映射可以被缓存
- 长期对话成本降低50-90%

---

## 脚本化与自动化

### 命令行脚本

```bash
# 单次任务执行
aider --message "添加类型注解到所有函数" src/utils.py

# 简写
aider -m "修复lint错误" src/**/*.py

# 自动确认所有操作
aider --yes-always --message "重构变量命名" src/
```

### Bash脚本示例

```bash
#!/bin/bash
# fix-all-types.sh

# 批量修复类型错误

for file in src/**/*.py; do
    echo "处理 $file..."

    aider --yes-always \
          --no-auto-commits \
          --message "添加完整的类型注解,使用typing模块" \
          "$file"

    # 运行mypy检查
    if mypy "$file"; then
        echo "✓ $file 类型检查通过"
    else
        echo "✗ $file 类型检查失败"
        exit 1
    fi
done

# 统一提交
git add src/
git commit -m "chore: 添加类型注解"
```

### Python API使用

```python
#!/usr/bin/env python3
"""
使用Aider的Python API批量处理
"""

import subprocess
import os

def run_aider_task(file_path, task):
    """运行aider任务"""
    cmd = [
        "aider",
        "--yes-always",
        "--message", task,
        file_path
    ]

    result = subprocess.run(
        cmd,
        capture_output=True,
        text=True
    )

    return result.returncode == 0

# 批量生成docstrings
files = [
    "src/models.py",
    "src/views.py",
    "src/utils.py"
]

task = """
为所有公共函数和类添加详细的docstring,
遵循Google风格指南,包括:
- 函数说明
- 参数描述(Args)
- 返回值(Returns)
- 可能的异常(Raises)
- 使用示例(Examples)
"""

for file in files:
    print(f"处理 {file}...")
    if run_aider_task(file, task):
        print(f"✓ {file} 完成")
    else:
        print(f"✗ {file} 失败")
```

### CI/CD集成

#### GitHub Actions示例

```yaml
# .github/workflows/aider-fixes.yml
name: Aider自动修复

on:
  push:
    branches: [develop]

jobs:
  auto-fix:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 需要完整历史

      - name: 设置Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 安装Aider
        run: pip install aider-chat

      - name: 运行自动修复
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          aider --yes-always \
                --model claude-3-5-sonnet-20250219 \
                --message "修复所有lint错误和类型问题" \
                src/**/*.py

      - name: 运行测试
        run: pytest tests/

      - name: 创建Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: Aider自动修复"
          title: "🤖 Aider自动修复"
          body: |
            此PR由Aider自动生成

            修复内容:
            - Lint错误
            - 类型注解问题

            请审查更改后合并
          branch: aider-auto-fix
          delete-branch: true
```

#### GitLab CI示例

```yaml
# .gitlab-ci.yml
aider-auto-docs:
  stage: document
  image: python:3.11
  script:
    - pip install aider-chat
    - |
      aider --yes-always \
            --message "为所有公共API生成文档" \
            src/**/*.py
    - git add docs/
    - git commit -m "docs: 自动生成API文档" || true
    - git push origin HEAD:auto-docs
  only:
    - master
```

---

## 最佳实践

### 1. 文件管理策略

**❌ 避免**:
```bash
# 添加整个代码库
aider .  # 会导致token爆炸
```

**✅ 推荐**:
```bash
# 只添加相关文件
aider src/auth.py src/models/user.py

# 其他文件作为只读参考
aider --read-only docs/api.md src/auth.py
```

**原则**:
- 只添加需要编辑的文件
- 使用 `--read-only` 添加参考文档
- 利用代码库映射,让AI自动找相关代码
- 定期用 `/drop` 清理不需要的文件

### 2. 上下文管理

```bash
# 定期检查token使用
> /tokens

# 当超过70%时:
# 1. 清理对话历史
> /clear

# 2. 移除不相关的文件
> /drop old_file.py

# 3. 或完全重置
> /reset
> /add src/current_task.py
```

### 3. Ask/Code混合工作流

```bash
# ============ 阶段1: 规划 ============
> /ask

> 我需要添加用户权限系统,支持角色和权限
> 给我几个设计方案

# AI提供方案A、B、C

> /ask 方案B看起来最好,详细说说实施步骤

# AI详细解释

# ============ 阶段2: 实施 ============
> /code

> 好的,按照方案B实施,先创建权限模型

# AI编辑代码

> 继续,添加角色模型

# AI继续编辑

# ============ 阶段3: 测试 ============
> /run pytest tests/test_permissions.py

# 根据测试结果调整

> 修复测试失败的那个权限检查逻辑
```

### 4. 增量开发

```bash
# ❌ 避免一次性大任务
> 实现完整的电商系统,包括用户、商品、订单、支付...

# ✅ 推荐拆分成小任务
> 1. 创建用户模型
> 2. 添加用户注册功能
> 3. 添加用户登录
> ...每个任务分开完成
```

**好处**:
- 每步都可以测试验证
- 出错容易定位和撤销
- Git历史清晰

### 5. Git提交策略

**策略A: 自动提交**(推荐用于探索性开发)
```yaml
# .aider.conf.yml
auto-commits: true
dirty-commits: false
```

**策略B: 手动提交**(推荐用于生产代码)
```yaml
auto-commits: false
```

```bash
# 完成任务后手动审查
> /diff

# 满意后手动提交
> /commit feat: 添加用户认证功能

# 或退出后用git
$ git add .
$ git commit -m "feat: 添加用户认证功能"
```

### 6. 模型选择策略

| 任务类型 | 推荐模型 | 原因 |
|---------|---------|------|
| 简单重构 | gpt-4o-mini | 快速、便宜 |
| 标准开发 | claude-3-5-sonnet | 平衡性能和成本 |
| 复杂架构 | o1-preview | 深度推理 |
| 大量代码生成 | deepseek | 性价比高 |
| 本地开发 | ollama/codellama | 隐私保护 |

**动态切换**:
```bash
# 启动时用强大模型做规划
aider --model o1-preview

# 规划后切换到便宜模型执行
> /model deepseek

# 遇到复杂问题再切回
> /model claude-3-5-sonnet
```

### 7. 提示词技巧

**❌ 模糊需求**:
```bash
> 改进这个函数
```

**✅ 具体需求**:
```bash
> 重构login函数:
> 1. 改用async/await
> 2. 添加重试逻辑(最多3次)
> 3. 使用exponential backoff
> 4. 添加详细日志
> 5. 添加类型注解
> 6. 添加单元测试
```

**✅ 提供上下文**:
```bash
> 我们的项目使用FastAPI + SQLAlchemy + PostgreSQL
> 请添加一个用户注册端点,要求:
> - 密码必须bcrypt加密
> - 邮箱必须验证格式
> - 返回JWT令牌
> - 遵循项目现有的错误处理模式
> - 添加对应的Pydantic模型
```

---

## 故障排查

### 问题1: Token超限

**症状**: 提示"Context length exceeded"

**解决**:
```bash
# 查看当前使用
> /tokens

# 清理历史
> /clear

# 移除不需要的文件
> /drop large_file.py

# 或重置
> /reset
```

### 问题2: 编辑失败

**症状**: Aider无法正确应用编辑

**解决**:
```bash
# 1. 确保文件格式正确
# 2. 尝试更简单的任务
# 3. 检查文件权限
chmod +rw file.py

# 4. 使用architect模式
> /architect 重试刚才的任务
```

### 问题3: Git冲突

**症状**: Aider提交失败

**解决**:
```bash
# 1. 检查工作区状态
$ git status

# 2. 如果有未提交的更改
$ git add .
$ git commit -m "WIP"

# 3. 使用dirty-commits
aider --dirty-commits
```

### 问题4: 模型响应慢

**解决**:
```bash
# 1. 切换到更快的模型
> /model gpt-4o-mini

# 2. 减少上下文
> /drop unnecessary_file.py

# 3. 使用本地模型
> /model ollama/codellama
```

### 问题5: API费用过高

**解决**:
```bash
# 1. 启用prompt缓存
# .aider.conf.yml
cache-prompts: true

# 2. 使用更便宜的模型
aider --model deepseek

# 3. 减少不必要的对话
# 使用/clear清理历史

# 4. 批量处理任务
aider --yes-always --message "..." files...
```

---

## 实战案例

### 案例1: 添加新功能

```bash
# 任务: 添加用户头像上传功能

# 1. 启动aider,添加相关文件
aider src/models/user.py src/routes/user.py

# 2. 先规划
> /ask

> 我需要添加用户头像上传功能
> 要求:
> - 支持JPG、PNG格式
> - 最大2MB
> - 存储到S3
> - 生成缩略图(100x100)
> 给我实施步骤

# 3. 根据建议实施
> /code

> 好的,先在User模型添加avatar_url字段

# 4. 逐步完成
> 添加上传路由 /users/me/avatar

> 添加图片验证和压缩逻辑

> 添加S3上传功能

> 添加对应的测试

# 5. 测试
> /run pytest tests/test_avatar_upload.py

# 6. 查看更改
> /diff

# 7. 完成
> /exit
```

### 案例2: 大规模重构

```bash
# 任务: 将同步代码改为异步

# 1. 使用architect模式
aider --architect --model o1-preview --editor-model deepseek

# 2. 添加关键文件
> /add src/database.py src/api.py src/services/*.py

# 3. 使用architect规划
> /architect

> 将整个应用改为async/await模式:
> 1. 数据库操作改用asyncpg
> 2. HTTP请求改用httpx
> 3. FastAPI路由改为async
> 4. 保持向后兼容
> 请提供详细的迁移计划

# Architect会生成详细方案
# Editor会逐步实施

# 4. 分阶段验证
> /run pytest tests/

# 5. 出错时调整
> 数据库连接有问题,修复一下
```

### 案例3: 代码审查与优化

```bash
# 1. 只读模式审查代码
aider --read-only src/performance_critical.py

# 2. 请求审查
> /ask 审查这个文件:
> 1. 性能瓶颈
> 2. 安全问题
> 3. 最佳实践违反
> 4. 可读性问题

# 3. 根据建议优化
> /code
> /add src/performance_critical.py

> 根据刚才的建议优化性能
```

---

## 快速参考

### 常用命令速查

```bash
# 启动
aider [文件...]                   # 基本启动
aider --model deepseek           # 指定模型
aider --yes-always               # 自动确认

# 文件管理
/add file.py                     # 添加文件
/drop file.py                    # 移除文件
/ls                              # 列出文件
/read-only doc.md                # 只读文件

# 模式切换
/code                            # 编辑模式
/ask                             # 询问模式
/architect                       # 架构师模式

# 上下文管理
/tokens                          # 查看token使用
/clear                           # 清理历史
/reset                           # 完全重置

# Git操作
/diff                            # 查看更改
/undo                            # 撤销更改
/commit                          # 手动提交

# 工具
/editor                          # 打开编辑器
/paste                           # 粘贴内容
/run command                     # 运行命令
/web URL                         # 添加网页

# 设置
/model name                      # 切换模型
/settings                        # 查看设置
/help                            # 帮助
/exit                            # 退出
```

### 配置模板

```yaml
# ~/.aider.conf.yml - 生产环境推荐配置

# 模型
model: claude-3-5-sonnet-20250219
editor-model: gpt-4o

# Git
auto-commits: false              # 手动控制提交
dirty-commits: false             # 要求干净工作区
attribute-commits: true          # 署名AI
commit-prompt: "生成简洁的commit消息,遵循Conventional Commits"

# 行为
yes-always: false                # 需要确认
stream: true                     # 流式输出
show-diffs: true                 # 显示差异
pretty: true                     # 美化输出

# 上下文
cache-prompts: true              # 启用缓存节省成本
map-tokens: 1024                 # 代码库映射token
watch-files: true                # 监控#AI注释

# 外观
dark-mode: true
```

---

## 总结

### Aider适合什么?

✅ **最适合**:
- CLI重度用户
- Git原生工作流
- 脚本化/自动化任务
- CI/CD集成
- 多模型灵活切换

✅ **优势**:
- 开源免费
- Git集成最佳
- 脚本化能力强
- 成本可控

⚠️ **限制**:
- 纯命令行,无GUI
- 需要熟悉终端操作
- 配置较复杂

### 下一步

1. **安装体验**: `pip install aider-chat`
2. **小项目测试**: 从简单任务开始
3. **学习命令**: 熟悉 `/add`, `/drop`, `/ask`, `/code`
4. **配置优化**: 创建 `.aider.conf.yml`
5. **集成工作流**: 加入CI/CD或日常开发

---

*参考资源*:
- 官方文档: https://aider.chat/docs/
- GitHub: https://github.com/Aider-AI/aider
- 选项参考: https://aider.chat/docs/config/options.html
