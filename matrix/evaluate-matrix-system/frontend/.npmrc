# NPM 配置文件
# 用于内网环境或无法下载原生二进制包的场景

# 跳过可选依赖安装（如 @rollup/rollup-win32-x64-msvc 等原生模块）
optional=false

# 如果内网有私有 npm 镜像，可以取消下面的注释并修改地址
# registry=http://your-internal-npm-registry.com/

# 跳过平台特定的可选依赖
omit=optional

